const path = require("path")

//DEVLOPMENT ONLY

// If using more than one entry, you can specify which output file will be restarted and what to watch.
const nodemonPluginConfig = {}

module.exports = { nodemonPluginConfig }
